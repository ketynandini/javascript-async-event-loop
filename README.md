JavaScript Async Event Loop

This project demonstrates how JavaScript handles synchronous and asynchronous operations using the event loop.

Features: 
Synchronous execution
setTimeout (macrotask)
Promise (microtask)
Async/await
Execution order comparison

This project is created to understand how JavaScript executes code behind the scenes, especially the event loop mechanism. 
